syntax="proto2";

// Packet going out from radio program to control program
message grSim_Status
{
    required uint32 robot_id = 1;
    
    optional bool ball_sensed = 2;
    optional bool just_kicked = 3;
    repeated int32 encoders = 4;
}
